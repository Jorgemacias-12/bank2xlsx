<UserControl x:Class="Bank2Pdf.Components.FileList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bank2Pdf.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Files}">

            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>

                <DataTemplate>

                    <Border Width="250"
                            Margin="10"
                            Padding="16"
                            CornerRadius="12"
                            Background="#1E1E1E"
                            BorderBrush="#333"
                            BorderThickness="1">

                        <StackPanel>

                            <TextBlock Text="{Binding Name}"
                                       FontSize="18"
                                       FontWeight="SemiBold"
                                       Foreground="White"
                                       TextTrimming="CharacterEllipsis"/>

                            <TextBlock Text="{Binding FullPath}"
                                       Margin="0 8 0 0"
                                       FontSize="12"
                                       Foreground="#AAA"
                                       TextWrapping="Wrap"/>

                            <TextBlock Margin="0 12 0 0"
                                       Foreground="#DDD">

                                <Run Text="Size: "/>
                                <Run Text="{Binding FormattedSize, Mode=OneWay}"/>
                                
                            </TextBlock>

                            <TextBlock Foreground="#DDD" Text="{Binding Status}"/>

                            <Button Content="Convertir"
                                Margin="0 16 0 0"
                                Padding="10 6"
                                Command="{Binding DataContext.ParseFileCommand,
                                                  RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}" />
                        </StackPanel>

                    </Border>

                </DataTemplate>

            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
